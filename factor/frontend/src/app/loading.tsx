export default function Loading() {
  return (
    <main className="factor-page" style={{ display: "grid", placeItems: "center", minHeight: "100vh" }}>
      <p style={{ fontFamily: "system-ui, sans-serif", color: "#475569" }}>Loading Factor…</p>
    </main>
  );
}
